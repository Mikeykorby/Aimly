<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Default Dynamic Resource values for M3 customization -->
    <SolidColorBrush x:Key="M3TabUnderlineBrush" Color="#AAFFFFFF"/>
    <SolidColorBrush x:Key="M3TooltipBackground" Color="#FF191919"/>
    <SolidColorBrush x:Key="M3TooltipForeground" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="M3TooltipAccentBrush" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="M3TooltipBorderBrush" Color="#40FFFFFF"/>
    <CornerRadius x:Key="M3TooltipCornerRadius">6</CornerRadius>

    <!-- TabControl Style with white underlines -->
    <Style x:Key="ModelsTabControl" TargetType="{x:Type TabControl}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
    </Style>

    <Style x:Key="ModelsTabItem" TargetType="{x:Type TabItem}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabItem}">
                    <Grid x:Name="templateRoot" SnapsToDevicePixels="True">
                        <Border x:Name="mainBorder"
                                Background="Transparent"
                                BorderThickness="0,0,0,2"
                                BorderBrush="Transparent"
                                Margin="0,0,0,0">
                            <ContentPresenter x:Name="contentPresenter"
                                              ContentSource="Header"
                                              Focusable="False"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="mainBorder" Property="BorderBrush" Value="{DynamicResource M3TabUnderlineBrush}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="mainBorder" Property="BorderBrush" Value="#55FFFFFF"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="mainBorder" Property="BorderBrush" Value="{DynamicResource M3TabUnderlineBrush}"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Modern Tooltip Style - Theme Integrated -->
    <Style x:Key="Tooltip" TargetType="ToolTip">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Background" Value="{DynamicResource M3TooltipBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource M3TooltipForeground}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="HasDropShadow" Value="True"/>
        <Setter Property="Placement" Value="Mouse"/>
        <Setter Property="HorizontalOffset" Value="12"/>
        <Setter Property="VerticalOffset" Value="12"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolTip">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="{DynamicResource M3TooltipCornerRadius}"
                            BorderThickness="1"
                            BorderBrush="{DynamicResource M3TooltipBorderBrush}">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="16"
                                              ShadowDepth="4"
                                              Opacity="0.5"
                                              Color="Black"/>
                        </Border.Effect>
                        <Grid>
                            <!-- Theme accent on left side -->
                            <Border CornerRadius="{DynamicResource M3TooltipCornerRadius}"
                                    HorizontalAlignment="Left"
                                    Width="3"
                                    Background="{DynamicResource M3TooltipAccentBrush}"/>
                            <!-- Content -->
                            <TextBlock Text="{TemplateBinding Content}"
                                       Foreground="{TemplateBinding Foreground}"
                                       FontSize="{TemplateBinding FontSize}"
                                       FontFamily="{TemplateBinding FontFamily}"
                                       TextWrapping="Wrap"
                                       MaxWidth="280"
                                       Margin="12,10,12,10"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
